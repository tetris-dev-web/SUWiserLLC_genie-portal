import React from 'react';

const VotesViewCapitalRaisedRect = ({x, y, height, fill}) => (
    <rect x={x} y={y} width="100%" height={height} fill={fill} opacity="0.5"></rect>
);

export default VotesViewCapitalRaisedRect;